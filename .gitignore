# Secrets — NEVER COMMIT
credentials.json
token.json
*.pem
.env
.env.*

# Local config (may contain personal defaults)
config.json

# Auto-tagging rules (personal data)
rules.json

# Sample data — local-only training and reference files
# (AMEX/WEX statements, JE training base, EIB templates, labor reports)
samples/

# AP/AR generated artifacts (rebuilt from samples/ via train_gl_*.py
# and grown at runtime as operators confirm classifications).
gl_memo_index.json
gl_merchant_map.json
cost_center_map.json
ap_tree_audit.json

# Logs
logs/

# Distribution archives (generated by `make handoff`)
dist/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Backup files (created during interactive edits)
*.bak.*
brand-voice.md.bak.*

# Pytest cache
.pytest_cache/
pytest-cache-files-*/

# Health reports (rotating)
logs/health_reports/
